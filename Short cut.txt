1. Create / Install react
	i. npx create-react-app filename

	ii. npm create vite@latest   (using vite rollup)
	    npm i (to install the node module)

2. Always try to make Component with First word capital letter. 
	like: Card.jsx

3. jsx ==> javascript fixed with HTML, bundler help to translate it 	  into browser html, css, js.

4. {} ==> evaluated expression , like in js `${}`

5. default react element predefined structure, done by BABEL

	React.createElement(
		'a',
		{href:.....}, //props
		'click me' // showed item
	)
		above is the predefined structure

6. hooks used for when i have to update multiplte value the value in       	UI, means UI update will control by React (hook) ===> solved 
	phantom problem. 

7. let [ var, Function name ] = useState(default value);
	to match the state value and UI represented value

8. tailwind css  ( to add do all in recommend in the site like- install, add in tailwind.css, add in index.css of decorator)

9. devUI io

10. react snippet extension
	rfce = react functional components

11. Router: npm i react-router-dom
	 import {createBrowserRouter} from 'react-router-dom';
	import {createBrowserRouter, RouterProvider} from 'react-	router-dom';

	*****DO IT IN MAIN.JSX****

12. Gurudev ar react-router-dom: (in main.jsx)

const router = createBrowserRouter(
  createRoutesFromElements(
    <Route path='/' element={<Layout />}>
      <Route path='' element={<Home/>} />
      <Route path='about' element={<About/>} />
      <Route path='contact' element={<Contact/>} />
  </Route>
  )
)

createRoot(document.getElementById('root')).render(
  <StrictMode>
    <RouterProvider router={router} />
  </StrictMode>,
)


13. Shadcn ==> new UI developing tool   (https://ui.shadcn.com/docs/dark-mode/vite)
	tailwind's daddy ==> Shandcn

14. TanStack

	==> without using useEffect, useState hooks for API fetcher when we more need
	use this 

	TanStack Query is a data-fetching and state management library for React (and now other 	frameworks too) that makes handling server state in your app a breeze.


15. <ReactQueryDevtools /> = a built-in, powerful DevTools panel for TanStack Query.
Lets you monitor, inspect, and debug all your queries in real-time right in your app.
📊 How it looks:
A small button appears at the bottom-left or bottom-right corner of your app.
Clicking it shows a panel listing all your queries, their cache, status, and data.


16. Geolocation: 
What is navigator.geolocation?
It’s a built-in browser API provided by the navigator object that lets you access the user’s geographical location (with their permission).

17. <Link> ==> Don't render the whole page like <a> tag

<button onclick="show()">Get Location</button>
<p id="result"></p>

<script>
function show() {
  if (navigator.geolocation) {
    navigator.geolocation.getCurrentPosition(
      function(position) {
        document.getElementById("result").innerHTML =
          "Latitude: " + position.coords.latitude + "<br>" +
          "Longitude: " + position.coords.longitude;
      },
      function(error) {
        document.getElementById("result").innerHTML =
          "Error: " + error.message;
      }
    );
  } else {
    document.getElementById("result").innerHTML = "Geolocation not supported.";
  }
}
</script>


17. For creating charts: Rechart library :  https://recharts.org/en-US/guide/installation

npm install recharts data-fns



    ******########## MAIN PROBLEM WAS TO PASS PROPS FROM PARENT TO VERY LOWER LEVEL CHILD #####*****


18. Cotext API for reducing the unnecessary props. Act like global variable.
	But context API only available in React, so for rest we have "Redux".
	Redux ===> help in overall state management very much.

19. Redux-toolkit (RTK) ==> easier version of React-Redux

20. For context API, we first do the 
	. Make UserContext.js file (for creating the context)

			import React from "react";
			const UserContext = React.createContext()
			export default UserContext;


	===>. make UserContextProvider.jsx file (for creating the provider like fragment to give context)

import React from "react";
import UserContext from "./UserContext";

//  children is like the inner div or content when used to enclose the code
const UserContextProvider = ({children}) =>{
    const [user, setUser] = React.useState(null)
    return (
        <UserContext.Provider value={{user,setUser}}>
            {children}  // like "oulet"
        </UserContext.Provider>
    )

}

export default UserContextProvider


****** the provider can be directed given into the App.jsx *******

21. for send data in context
	in login page

const {setUser} = useContext(UserContext);

    const handleSubmit=(e)=>{
        e.preventDefault()
        setUser({username,password})    // in user state we are storing object stored=>(username,password)		
        
    }

22. for profile page

import React, { useContext } from "react"
import UserContext from "../context/UserContext"

const Profile = () => {
    const {user} = useContext(UserContext)
  
    if(!user) return <div>Please Login</div>

    return <div>welcome {user.username}</div>
}
export default Profile



23. *****#### Doing the two files (UserContextProvider nd UserContext) can done in signle file ######********

file==> Theme.js (in 09themeswitcher) 

import { createContext, useContext } from "react";

// Create context but this time we are giving the default value
export const ThemeContext = createContext({
    themeMode: "light",
    darkTheme: ()=>{},
    lightTheme: ()=>{},
})

// making context provider into same file
export const ThemeProvider = ThemeContext.Provider

// for reducing all time import context 
export default function useTheme(){
    return useContext(ThemeContext)
}


24. for darkmode in tailwind
	add in index.css
	==> @custom-variant dark (&:where(.dark, .dark *));

25. const hasNull = [firstname, lastname, address].some(item => item == null); trick
26. localStorage stored value in => key: value format
	localStorage always stored value is String format so use JSON.stringify()
	and for getting use JSON.parse() [for convert into JSON format]
27. Context Api is like --> Global storage for variable --> use useContext hook and use it



####################||  REDUX and REDUX-TOOLKIT and REACT-REDUX  ||###########################


28. Redux is a library --> For using with react we use= React-library 
29. Setting up:
	https://redux.js.org/introduction/getting-started
30. Slice is the big version of Reducer in Redux in Redux-toolkit
31. In Context api we can do the definition later but in redux we have to do it in the time of declaration 

32. useDispatch() uses ==> a reducer to change in ==> Store
	// useDispatch() uses ==> a reducer to change in ==> Store

33. useSelector() uses ==> for select the data from store=> From store but keep from state

34. create a store=> create feature (slice)[unlike context api, reducer definition should be done here ] ==> give the reducer list to store => then take provide and wrap in main/App and give value of Store
 
		<=== code ==>
import { configureStore } from "@reduxjs/toolkit";
import todoReducer from '../features/todo/todoSlice'  

export const store = configureStore({
    reducer: todoReducer
}) 



     ***********todoSlice.js ***********

import { createSlice, nanoid } from "@reduxjs/toolkit";
//  nanoid ==> for generating the unique id 


// At intital the Store will have inital values so
const initialState = {
    todos: [{id: 1, text: "Hello World"}]
}
 
// Slice is the big version of reducer
export const todoSlice = createSlice({
    name: 'todo', // this name will show in chrome extension 
    initialState,  
    // in Context api we can do the definition later but in redux we have to do it in the time of declaration

    reducers: {
        
        // state=> give inital value  action=> given by us, like to remove Todo we need to give the id 
        addTodo: (state, action)=> {
            const todo = {
                id: nanoid(),
                text: action.payload // payload is a object which can contain anything
            }
            state.todos.push(todo)
        }, 
        removeTodo: (state, action)=>{
            state.todos = state.todos.filter((todo)=> todo.id !== action.payload )
        },
    }
})

export const { addTodo, removeTodo } = todoSlice.actions

export default todoSlice.reducer



 ***********AddTOdo.jsx ***********
import { useState } from "react"
import {useDispatch} from 'react-redux'
import { addTodo } from "../features/todo/todoSlice"

const AddTodo = () => {
    const [input, setInput] = useState("")
    // useDispatch() uses ==> a reducer to change in ==> Store
    const dispatch = useDispatch()

    const addTodoHandler = (e) =>{
        e.preventDefault()
        dispatch(addTodo(input)) //  // useDispatch() uses ==> a reducer to change in ==> Store
        setInput('')
    }

  return (
    <form onSubmit={addTodoHandler} className="space-x-3 mt-12">
        <input type="text" className="bg-gray-800 rounded border border-gray-700 focus:border-indigo-500 focus:ring-2 focus:ring-indigo-900 text-base outline-none text-gray-100 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out" 
        placeholder="Enter a Todo..."
        value={input}
        onChange={(e)=> setInput(e.target.value)}
        />
        <button type="submit" className="text-white bg-indigo-500 border-0 py-2 px-6 focus:outline-none hover:bg-indigo-600 rounded text-lg">
            Add Todo
        </button>
    </form>
  )
}
export default AddTodo

 ***********Todos.jsx ***********
import React from 'react'
import { useSelector, useDispatch } from 'react-redux'
import { removeTodo } from '../features/todo/todoSlice'

const Todos = () => {
    const todos = useSelector(state => state.todos) // FOR selecting the stored todos
    const dispatch = useDispatch()
  return (
    <>
        <div> Todos </div>
        <ul className='list-none'>
        {todos.map((todo) => (
            <li
            className='mt-4 flex justify-between items-center bg-zinc-800 px-4 py-2 rounded'
            key={todo.id}
            >
                <div className='text-white'> {todo.text} </div>
                <button
                onClick={() => dispatch(removeTodo(todo.id))}
                className='text-white bg-red-500 border-0 py-1 px-4 focus:outline-none hover:bg-red-600 rounded text-md'
                > X </button>
            </li>
        ))}
        </ul>
    </>
  )
}

export default Todos



####################||  REACT MEGA PROJECT ||###########################


	# Installation all:
1. https://www.npmjs.com/package/@reduxjs/toolkit
	npm install @reduxjs/toolkit
	npm install react-redux
2. npm i react-router-dom 	appwrite

3. https://www.npmjs.com/package/@tinymce/tinymce-react [Visual Code Editor]
	npm i @tinymce/tinymce-react

4. https://www.npmjs.com/package/html-react-parser [HTML tag to normal, because Appwrite give like that]
		npm install html-react-parser
	
5. https://react-hook-form.com/get-started
 npm install react-hook-form

6. https://vite.dev/guide/env-and-mode
	VITE_SOME_KEY=123
	DB_PASSWORD=foobar

	console.log(import.meta.env.VITE_SOME_KEY) // "123"
	console.log(import.meta.env.DB_PASSWORD) // undefined
7. *** Must add permission in collection user access

8. create appwrite--> auth.js
	[ For creating authentication like register, login, logout etc]
9. create state for redux Store for state management

10. in App.js we do 
	1. first fetch data from autService [appwrite service]
	2. then pass it to reducer's actions for state management in redux store


11. When to use forwardRef?

When you need to pass a ref from a parent component to a child functional component’s DOM node.

Common in cases like:

	Custom Input components

	Modals/Portals

	Animations

	Third-party integrations that need DOM access
exp--> megaBlog project==> Select.jsx and Input.jsx


12. react-hook-form
	https://youtu.be/lfMyCuB6xfc?list=PLu71SKxNbfoDqgPchmvIsL4hTnJIrtige&t=1669
	Login.jsx (in 12MegaBlog)
	
13. RegexP
	https://regexr.com/3a4dg

14. http://localhost:3000/profile
	then
*******	Relative Path

	<Link to="profile" />
	navigate("profile")		==> http://localhost:3000/dashboard/profile

*******	Absolute Path
	<Link to="/profile" />
	navigate("/profile")            ==> http://localhost:3000/profile


	






